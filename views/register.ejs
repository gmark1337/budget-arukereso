<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title><%= p.user.register[lang] %></title>

    <link rel="stylesheet" href="theme.css" />
    <link rel="stylesheet" href="auth.css" />
  </head>
  <body class="auth-body">
    <div class="auth-modal">
      <div class="auth-header">
        <h1><%= p.user.register[lang] %></h1>
        <!-- X gomb: vissza a kezdőoldalra -->
        <a href="/?lang=<%= lang %>" class="auth-close">✕</a>
      </div>

      <form method="POST" action="" class="auth-form">
        <input type="hidden" name="lang" value="<%= lang %>">
        <div class="auth-field">
          <label for="email"><%= p.user.email[lang] %></label>
          <input type="email" id="email" name="email" autocomplete="email">
        </div>

        <div class="auth-field">
          <label for="username"><%= p.user.username[lang] %></label>
          <input type="text" id="username" name="username" autocomplete="username">
        </div>

        <div class="auth-field">
          <label for="password"><%= p.user.password[lang] %></label>
          <input type="password" id="password" name="password" autocomplete="new-password">
        </div>

        <button type="submit" class="auth-submit">
          <%= p.user.register[lang] %>
        </button>

        <% if (errorMessage) { %>
          <div class="auth-error"><%= errorMessage %></div>
        <% } %>

        <p class="auth-switch">
            <%= p.user.tologin[lang] %>
            <a href="/login?lang=<%= lang %>"><%= p.user.login[lang] %></a>
        </p>

      </form>
    </div>
  </body>
</html>
