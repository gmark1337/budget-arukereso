<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title><%= user.register[lang] %></title>
    </head>
    <body>
        <form method="POST" action="">
            <label for="email"><%= p.user.email[lang] %></label>
            <input type="email" id="email" name="email">
            <label for="username"><%= p.user.username[lang] %></label>
            <input type="text" id="username" name="username">
            <label for="password"><%= p.user.password[lang] %></label>
            <input type="password" id="password" name="password">
            <input type="submit" value="<%= p.user.register[lang] %>">
        </form>
        <% if (errorMessage) { %>
        <div id="error-message">
            <%= errorMessage %>
        </div>
        <% } %>
    </body>
</html>
