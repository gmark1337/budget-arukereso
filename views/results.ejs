<% results.map((result) => { %>
  <fieldset class="result">
    <legend><%= result.websiteName %></legend>
    <div class="items">
      <% if (result.FoundImages.length == 0) { %>
        <%= p.search.notfound[lang] %>
      <% } %>

      <% result.FoundImages.map((item) => { %>
        <div class="item">
            <img class="path-<%= item.href %>" src="<%= item.src || 'https://via.placeholder.com/300x300?text=Image' %>" alt="">
            <% if (auth) { %>
              <button class="favourite-add <%= item.fav %> id-<%= item.fav_id %>">♥</button>
            <% } %>
          <div class="meta">
            <div class="title"><%= item.title || 'Product' %></div>
            <div class="price"><span class="chip"><%= item.price %> Ft</span></div>
          </div>
        </div>
      <% }) %>
    </div>
  </fieldset>
<% }) %>
