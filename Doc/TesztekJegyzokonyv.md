| Sorszám | Funkció leírása | Vizsgálat módja/eszköze | Elvárt eredmény | Eredmény | Megfelelősség státusza
  :------ | :-------------: | :---------------------: | :-------------: | :------: | :--------------------:
| Átalános funkcionális teszt
| 1. | A search függvény a beállított darabszámmal tér vissza minde oldal esetén. | A keresési darabszámot weboldalaként beállítjuk 4 darabra, majd ellenőrizzük a tömbök hosszát | 4 | 4 | **MEGFELELT**
| 2. | A `search` függvény figyelemebe veszi a `blacklist` paraméter értékét, amivel letiltott boltok állíthatóak be. | A Hervis kivételével letiltunk minden boltot, majd ellenőrizzük hogy egyedül a Hervis adott vissza eredményt. | Hervis | Hervis | **MEGFELELT**
| 3. | A `search` függvényhez megadhatunk ár intervallumot amivel pontosítani tudjuk a keresést. | A beérkezett termékek mindegyikénél ellenőrizzük hogy a definiált intervallumon belül vannak, megengedő módon. | true | true | **MEGFELELT**
| 4. | A `/search` endpoint a találatokkal kirenderelt HTML templatet küldi vissza válaszul, ahol meghatározhatunk termékt darabszámot. | Elküldünk egy véletlenszerűen generált paraméterekkel ellátott lekérdezést majd ellenőrizzük, hogy a válasz HTML-ben lévő termékek össszes darabszáma megegyezik-e az elküldött darabszámok összegével. | 9 | 9 | **MEGFELELT**
| 5. | A `/search` endpoint a találatokkal kirenderelt HTML templatet küldi vissza válaszul, ahol meghatározhatunk ár intervallumot. | Véletlenszerűen generálunk egy alsó értéket 1000 és 10.000 között, illetve felső értéket 11.000 és 20.000 között, majd ellenőrizzük hogy minden beérkezett termékaz intervallumon belül van megengedő módon. | true | true | **MEGFELELT**
| 6. | A `/search` endpoint a találatokkal kirenderelt HTML templatet küldi vissza válaszul, ahol meghatározhatunk rendezési módot ár szerint. | Véletlenszerűen elkülünk egy kérést, ahol `order` paraméter vagy `asc` vagy `desc` értékkel rendelkezik, majd a beérkező HTML-ből kiszedjük az árakat, rendezzük a paraméter szerint és megnézzük hogy deepEqual igaz-e a két tömbre. | true | true | **MEGFELELT**
| 7. | A `/register` endpointra elküldött `email, username, password` hármassal képes a felhasználó regisztrálni. | Elküldünk egy **POST** kérést az endpointra ahol a username értéke test-user-bob, majd megnézzük hogy a felhasználó táblában létezik-e ilyen felhasználó. | test-user-bob | test-user-bob | **MEGFELELT**
| 8. | A `/login` endpointra elküldött `username, password` párossal már regisztrált felhasználóval be lehet lépni az oldalra. Sikeres belépés esetén egy átirányító státusz kódot kell kapjunk. | Elküldünk egy **POST** kérést az endpointra a megfelelő értékekkel, majd ellenőrizzük, hogy a visszakapott státusz kód átírányít-e. | 302 | 302 | **MEGFELELT**
| 9. | Bejelentkezés után az oldal jobb felső sarokban megjeleníti a belépett felhasználó nevét. | Elküldünk egy **POST** kérést a `/login` endpointra, majd a válaszul kapott HTML-ben megkeressük a `div#account` értékét. | test-user-bob | test-user-bob | **MEGFELELT**
| 10. | A `/history` endpoint lehetőséget ad arra hogy eltároljuk az előzményeket. | Elküldünk egy **POST** kérést az endpointra, majd ellenőrizzük hogy a kérést előtti history elemek darabszáma, a kérés után eggyel nőtt. | true | true | **MEGFELELT**
| 11. | A `/history` endpoint lehetőséget ad arra hogy megnézzük az előzményeket. | Elküldünk egy **POST** kérést az endpointra, majd elküldünk egy  **GET** kérést az endpointra és a visszakapott HTML-ben megnézzük hogy jelen van-e a hozzáadott termék. | true | true | **MEGFELELT**
| 12. | A `/history` endpoint lehetőséget ad arra hogy töröljünk az előzmények közül. | Elküldünk egy **POST** kérést az endpointra, majd elküldünk egy **DELETE** kérést az endpointra és megnézzük hogy a kéréset előtti darabszám nem változott-e. | true | true | **MEGFELELT**
| 13. | A `/history` endpoint hozzáadaás előtt ellenőrzi, hogy nem lépjük-e túl a maximum 10 darab terméket. | Elküldünk 15 **POST** kérést az endpointra, majd megnézzük hogy a darabszám nem haladja-e meg a 10-et. | true | true | **MEGFELELT**
| 14. | A `/history` endpoint hozzáadaás előtt ellenőrzi, hogy nem tartalmazza-e már az adott terméket a lista. | Elküldjük kétszer ugyan azt a **POST** kérést, majd megnézzük, hogy a kérések előtti darabszám csak eggyel nőtt-e. | true | true | **MEGFELELT**
| 15. | A `/favourites` endpoint lehetőséget ad arra, hogy elmentsük későbbre a nekünk tetsző termékeket. | Elküldünk egy **POST** kérést az endpointra, majd ellenőrizzük hogy a kérést előtti favourites elemek darabszáma, a kérés után eggyel nőtt. | true | true | **MEGFELELT**
| 16. | A `/favourites` endpoint lehetőséget ad arra, hogy töröljünk az kedvezmények közül. | Elküldünk egy **POST** kérést az endpointra, majd elküldünk egy **DELETE** kérést az endpointra és megnézzük hogy a kéréset előtti darabszám nem változott-e. | true | true | **MEGFELELT**
| 17. | A `/favourites` endpoint lehetőságet ad arra, hogy megtekintsük az elmentett kedvenceinket. | Elküldünk egy **POST** kérést az endpointra, majd elküldünk egy **GET** kérést az endpointra, majd ellenőrizzük hogy a beérkezett HTML válasz tartalmaz egy olyan képet aminek a src attribútuma megegyezik-e az elküldöttel. | true | true | **MEGFELELT**
| 18. | A `/favourites` endpoint hozzáadaás előtt ellenőrzi, hogy nem lépjük-e túl a maximum 10 darab terméket. | Elküldünk 15 **POST** kérést az endpointra, majd megnézzük hogy a darabszám nem haladja-e meg a 10-et. | true | true | **MEGFELELT**
| 19. | A `/reviews` endpoint lehetőséget ad arra, hogy értékeljük a különböző boltokat. | Elküldünk egy **POST** kérést az endpointra, majd ellenőrizzük hogy a kérést előtti reviews elemek darabszáma, a kérés után eggyel nőtt. | true | true | **MEGFELELT**
| 20. | A `/reviews` endpoint lehetőséget ad arra, hogy csillagok formájában értékeljük a boltokat. | Elküldünk egy **POST** kérést az endpointra, majd egy **GET** kérést és megnézzük hogy a frissen hozzáadott vélemény hány darab csillaggal rendelkezik. | 5 | 5 | **MEGFELELT**
| 21. | A `/reviews` endpoint lehetőséget ad arra, hogy látható legyen kik milyen véleményt írtak az adott boltról. | Elküldünk egy **POST** kérést az endpointra, majd egy **GET** kérést és megnézzük hogy a frissen hozzáadott véleménynél megjelenik-e a felhasználónév. | test-user-stanley | test-user-stanley | **MEGFELELT**
| 22. | A `/reviews` endpoint lehetőséget ad arra, hogy egy bizonyos jó értékelés elérése után a bolt elnyerheti a *Trusted site.* jelvényt. | Elküldünk 6 darab **POST** kérést az endpointra, majd egy **GET** kérést és megnézzük hogy a bolt amihet hozzáadtuk a véleményeket tartalmaz-e `.trusted-site` element-et és lekérjük az értékét. | Trusted site | Trusted site | **MEGFELELT**
| Szélsőérték funkcionális teszt
| 1. | A search függvény találat hiányában egy üres tömböt ad vissza. | A Sinsay weboldal nem ad vissza eredményt az `adidas` kulcsszóra, így beállítjuk azt egyetlen boltnak, majd megnézzük hogy üres-e a visszakapot tömb. | [] | [] | **MEGFELELT**
| 2. | A `/search` endpoint elküldi a paraméterek a `search` függvénynek majd visszakapja a kirenderelt HTML template-et a talált termékekkel. | A Sinsay weboldal nem ad vissza eredményt az `adidas` kulcsszóra, így beállítjuk azt egyetlen boltnak, majd megnézzük hogy a kirenderelt HTML a nincs találat hibaüzenetet tartalmazza. | Failed to find products for this website! | Failed to find products for this website! | **MEGFELELT**
| 3. | A `/search` endpoint üres kulcsszavat is tud továbbítani a `search` függvény felé. | A `search` függvény üres kulcsszó használata esetén azonnal visszaküld egy üres tömbböt, amit a webszerver érzékel és egy üres stringet küld vissza a response-ban. | "" | "" | **MEGFELET**
| 4. | A `/search` endpoint undefined értéket is tud továbbítani a `search` függvény felé. | A `search` függvény undefined kulcsszó használata esetén azonnal visszaküld egy üres tömbböt, amit a webszerver érzékel és egy üres stringet küld vissza a response-ban. | "" | "" | **MEGFELET**
| 5. | A `/search` endpoint értelmetlen kulcsszavat is tud továbbítani a `search` függvény felé. | Elküldjük a `aggrgeththeasdsdfbafvguvguasrf` kulcsszavat az endpointon kereszült a webszerver felé, ami találat hiányában a nincs találattal kirenderelt HTML üzenetet küldi vissza. | Failed to find products for this website! | Failed to find products for this website! | **MEGFELELT**
| 6. | A `/register` endpoint hiányos paramét esetén hibával tér vissza. | Regisztrálásnál nem adunk meg username mező értéket. Majd megnézzük a hibaüzenetet | No username provided | No username provided | **MEGFELELT**
| 7. | A `/register` endpoint hiányos paramét esetén hibával tér vissza. | Regisztrálásnál nem adunk meg password mező értéket. Majd megnézzük a hibaüzenetet | No password provided | No password provided | **MEGFELELT**
| 8. | A `/register` endpoint hiányos paramét esetén hibával tér vissza. | Regisztrálásnál nem adunk meg email mező értéket. Majd megnézzük a hibaüzenetet | No email provided | No email provided | **MEGFELELT**
| 9. | A `/register` endpoint foglalt email esetén hibával tér vissza. | Regisztrálásnál foglalt email mező értéket adunk meg. Majd megnézzük a hibaüzenetet | Email taken | Email taken | **MEGFELELT**
| 10. | A `/register` endpoint foglalt username esetén hibával tér vissza. | Regisztrálásnál foglalt username mező értéket adunk meg. Majd megnézzük a hibaüzenetet | Username taken | Username taken | **MEGFELELT**
| 11. | A `/login` endpoint hibás jelszó esetén hibával tér vissza. | Bejelentkezésnél rossz password mező értéket adunk meg. Majd megnézzük a hibaüzenetet | Invalid password | Invalid password | **MEGFELELT**
| 12. | A `/login` endpoint nem létező felhasználónév esetén hibával tér vissza. | Bejelentkezésnél nem létező felhasználónevet adunk meg username mező értéknek. Majd megnézzük a hibaüzenetet | Invalid username | Invalid username | **MEGFELELT**
| Biztonsági teszt
| 1. | A `/history` endpoint használatához szükséges hogy a felhasználó be legyen jelentkezve. | Elküldünk egy **POST** kérést az endpointra bármi féle autentikációs süti nélkül majd ellenőrizzük a válasz json `reason` mező értékét. | unauthorized | unautohrized | **MEGFELELT**
| 2. | A `/favourites` endpoint használatához szükséges hogy a felhasználó be legyen jelentkezve. | Elküldünk egy **POST** kérést az endpointra bármi féle autentikációs süti nélkül majd ellenőrizzük a válasz json `reason` mező értékét. | unauthorized | unautohrized | **MEGFELELT**
| 3. | A `/reviews` endpoint használatához szükséges hogy a felhasználó be legyen jelentkezve. | Elküldünk egy **POST** kérést az endpointra bármi féle autentikációs süti nélkül majd ellenőrizzük a válasz json `reason` mező értékét. | unauthorized | unautohrized | **MEGFELELT**
